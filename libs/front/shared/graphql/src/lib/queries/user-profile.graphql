query getUserProfile($key: String!, $value: String!) {
  userProfile(key: $key, value: $value) {
    id
    createdAt
    bannerUrl
    profileUrl
    pseudo
    punchline
    userName
    userId
  }
}
